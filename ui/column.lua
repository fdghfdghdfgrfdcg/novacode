local TweenService = game:GetService("TweenService")

return function(COLORS, Round, Main, title, x, items, callbacks)
    -- сюда копируешь всё тело CreateColumn
    -- заголовок return Column в конце оставляешь
end
